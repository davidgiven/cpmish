language: generic

compiler:
    - gcc

os:
    - linux

dist:
    - xenial

addons:
    apt:
        packages:
        - sdcc
        - netpbm
        - cpmtools
        - libstb-dev
        - lua5.1
        - ninja-build

script:
    - make

